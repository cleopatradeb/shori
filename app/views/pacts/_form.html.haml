= form_for @pact do |f|
  - if @pact.errors.any?
    #error_explanation
      %h2= "#{pluralize(@pact.errors.count, "error")} prohibited this pact from being saved:"
      %ul
        - @pact.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
