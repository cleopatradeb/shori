%h1 Listing pacts

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @pacts.each do |pact|
      %tr
        %td= link_to 'Show', pact
        %td= link_to 'Edit', edit_pact_path(pact)
        %td= link_to 'Destroy', pact, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Pact', new_pact_path
