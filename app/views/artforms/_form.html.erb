<%= form_for(@artform) do |f| %>
  <% if @artform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artform.errors.count, "error") %> prohibited this artform from being saved:</h2>

      <ul>
      <% @artform.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
