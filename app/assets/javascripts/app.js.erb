var app = angular.module('shoriApp', ['ngAnimate', 'ngRoute', 'ngResource', 'angular.filter', 'angular-datepicker']);
console.log('ANGULAR RUNNING');

app.config(function($routeProvider, $locationProvider){
  $routeProvider
  .when('/home', {
    controller: "HomeController",
    templateUrl: "<%= asset_path('home.html') %>"
  })
  .when('/dashboard', {
    controller: "DashboardController",
    templateUrl: "<%= asset_path('dashboard.html') %>"
  })
  .when('/browse', {
    controller: "BrowseController",
    templateUrl: "<%= asset_path('browse.html') %>"
  })
  .when('/users/:id', {
    controller: 'ProfileController',
    templateUrl: '<%= asset_path('profile.html') %>'
  })
  .otherwise({
    redirectTo: '/home'
  });
  $locationProvider.html5Mode(true);
});
